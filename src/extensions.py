# Импортируем логгер для записи событий расширений
from src.logger import setup_logger

# Создаём логгер для модуля расширений
logger = setup_logger("extensions")


# Создаём класс Extensions — заготовка для подключения сторонних сервисов
# Здесь будут методы для интеграции с Telegram, веб-сайтом и другими сервисами
class Extensions:
    """
    Класс-заготовка для подключения сторонних сервисов.

    Текущая версия содержит:
    - Заготовку для Telegram-бота
    - Заготовку для веб-API
    - Заготовку для мобильного приложения

    Все методы в текущей версии — заглушки.
    Для активации раскомментируйте соответствующие зависимости в requirements.txt.
    """

    # Конструктор — принимает объект бизнес-логики для доступа к данным
    def __init__(self, logic=None) -> None:
        """
        Инициализирует модуль расширений.

        Аргументы:
            logic: объект BusinessLogic (по умолчанию None)
        """
        # Сохраняем ссылку на бизнес-логику
        self.logic = logic

        # Логируем создание объекта расширений
        logger.info("Модуль расширений инициализирован")

    # === TELEGRAM-БОТ ===

    # Метод: запустить Telegram-бота (заглушка)
    def start_telegram_bot(self, token: str) -> None:
        """
        Запускает Telegram-бота для управления заметками.

        Аргументы:
            token: токен Telegram-бота от BotFather

        Для активации:
        1. Раскомментируйте строку в requirements.txt
        2. Установите зависимости: pip install -r requirements.txt
        3. Замените заглушку на реальную реализацию
        """
        # Заглушка — выводим сообщение
        logger.info("Telegram-бот: функция в разработке")
        print("  ℹ️  Telegram-бот будет доступен в следующей версии.")

    # === ВЕБ-API ===

    # Метод: запустить веб-сервер (заглушка)
    def start_web_api(self, host: str = "0.0.0.0", port: int = 8080) -> None:
        """
        Запускает веб-сервер для доступа к заметкам через браузер.

        Аргументы:
            host: адрес для привязки (по умолчанию 0.0.0.0 — все интерфейсы)
            port: порт сервера (по умолчанию 8080)

        Для активации:
        1. Раскомментируйте строку в requirements.txt
        2. Установите зависимости: pip install -r requirements.txt
        3. Замените заглушку на реальную реализацию
        """
        # Заглушка — выводим сообщение
        logger.info("Веб-API: функция в разработке")
        print("  ℹ️  Веб-API будет доступен в следующей версии.")

    # === ЭКСПОРТ ДАННЫХ ===

    # Метод: экспортировать заметки в формат CSV (заглушка)
    def export_to_csv(self, output_path: str = "export.csv") -> bool:
        """
        Экспортирует все заметки в CSV-файл.

        Аргументы:
            output_path: путь для сохранения CSV-файла

        Возвращает:
            True если экспорт прошёл успешно, False при ошибке

        Для активации: реализуйте запись данных в CSV
        """
        # Заглушка — выводим сообщение
        logger.info("Экспорт в CSV: функция в разработке")
        print("  ℹ️  Экспорт в CSV будет доступен в следующей версии.")
        return False
